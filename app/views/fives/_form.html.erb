<%= form_for(@fife) do |f| %>
  <% if @fife.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fife.errors.count, "error") %> prohibited this fife from being saved:</h2>

      <ul>
      <% @fife.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
