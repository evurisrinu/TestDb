<%= form_for(@twentyfife) do |f| %>
  <% if @twentyfife.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@twentyfife.errors.count, "error") %> prohibited this twentyfife from being saved:</h2>

      <ul>
      <% @twentyfife.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
