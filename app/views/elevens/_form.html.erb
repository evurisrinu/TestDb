<%= form_for(@eleven) do |f| %>
  <% if @eleven.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@eleven.errors.count, "error") %> prohibited this eleven from being saved:</h2>

      <ul>
      <% @eleven.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
