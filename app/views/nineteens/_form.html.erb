<%= form_for(@nineteen) do |f| %>
  <% if @nineteen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nineteen.errors.count, "error") %> prohibited this nineteen from being saved:</h2>

      <ul>
      <% @nineteen.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
