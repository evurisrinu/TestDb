<%= form_for(@eightteen) do |f| %>
  <% if @eightteen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@eightteen.errors.count, "error") %> prohibited this eightteen from being saved:</h2>

      <ul>
      <% @eightteen.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
