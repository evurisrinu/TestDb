<%= form_for(@thirteen) do |f| %>
  <% if @thirteen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thirteen.errors.count, "error") %> prohibited this thirteen from being saved:</h2>

      <ul>
      <% @thirteen.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
