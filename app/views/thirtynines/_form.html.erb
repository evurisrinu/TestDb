<%= form_for(@thirtynine) do |f| %>
  <% if @thirtynine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thirtynine.errors.count, "error") %> prohibited this thirtynine from being saved:</h2>

      <ul>
      <% @thirtynine.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
