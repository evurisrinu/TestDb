<%= form_for(@two) do |f| %>
  <% if @two.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@two.errors.count, "error") %> prohibited this two from being saved:</h2>

      <ul>
      <% @two.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
