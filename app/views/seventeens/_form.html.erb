<%= form_for(@seventeen) do |f| %>
  <% if @seventeen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seventeen.errors.count, "error") %> prohibited this seventeen from being saved:</h2>

      <ul>
      <% @seventeen.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
