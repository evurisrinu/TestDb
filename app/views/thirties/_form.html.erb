<%= form_for(@thirty) do |f| %>
  <% if @thirty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thirty.errors.count, "error") %> prohibited this thirty from being saved:</h2>

      <ul>
      <% @thirty.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
