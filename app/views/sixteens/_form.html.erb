<%= form_for(@sixteen) do |f| %>
  <% if @sixteen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sixteen.errors.count, "error") %> prohibited this sixteen from being saved:</h2>

      <ul>
      <% @sixteen.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
