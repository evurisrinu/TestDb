<%= form_for(@twelf) do |f| %>
  <% if @twelf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@twelf.errors.count, "error") %> prohibited this twelf from being saved:</h2>

      <ul>
      <% @twelf.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
