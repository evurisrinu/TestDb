<%= form_for(@six) do |f| %>
  <% if @six.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@six.errors.count, "error") %> prohibited this six from being saved:</h2>

      <ul>
      <% @six.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
