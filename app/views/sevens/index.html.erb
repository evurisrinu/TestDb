<h1>Listing sevens</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @sevens.each do |seven| %>
      <tr>
        <td><%= seven.name %></td>
        <td><%= link_to 'Show', seven %></td>
        <td><%= link_to 'Edit', edit_seven_path(seven) %></td>
        <td><%= link_to 'Destroy', seven, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Seven', new_seven_path %>
