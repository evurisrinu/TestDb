<%= form_for(@twentyeight) do |f| %>
  <% if @twentyeight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@twentyeight.errors.count, "error") %> prohibited this twentyeight from being saved:</h2>

      <ul>
      <% @twentyeight.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
